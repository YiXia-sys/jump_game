# 任务列表：竞技模式（回合制多人赛跑对战）

## 任务 1：Mulberry32 伪随机数生成器

- [x] 在 `index.html` 中实现 `mulberry32(seed)` 函数，接受 32 位整数种子，返回一个每次调用产生 [0, 1) 浮点数的函数
- [x] 实现 `seededRandom(prng, min, max)` 辅助函数，基于 PRNG 生成指定范围的整数
- [x] 验证：相同种子产生完全相同的序列

> 需求：4.2, 4.3 | 属性：1

## 任务 2：确定性赛道生成

- [x] 实现 `generateRacetrack(seed)` 函数，使用 Mulberry32 生成 20 个平台的赛道
- [x] 平台参数（位置、宽度、形状、间距、方向）全部由 seeded PRNG 决定
- [x] 第 1 个平台为起点，第 20 个为终点，均不设为 Mystery_Platform
- [x] 第 2-19 个平台以 20% 概率标记为 Mystery_Platform
- [x] 特殊平台（移动/缩小/限时）复用经典模式逻辑，互斥分配

> 需求：4.1-4.6, 7.1 | 属性：2

## 任务 3：游戏服务器基础框架 (`server.js`)

- [x] 创建 `server.js`，使用 Node.js + `ws` 库启动 WebSocket 服务，默认端口 3000
- [x] 实现消息路由：解析 JSON 消息，按 `type` 字段分发到对应处理函数
- [x] 实现连接管理：为每个连接分配唯一玩家 ID，发送 `welcome` 消息
- [x] 实现心跳检测：5 秒超时判定断线
- [x] 格式错误消息返回 `error` 类型响应

> 需求：2.1-2.3, 2.6, 2.7 | 属性：4, 22

## 任务 4：房间管理器

- [x] 实现 `RoomManager` 类：`createRoom(hostId)`、`joinRoom(roomCode, playerId, playerName)`、`removePlayer(playerId)`、`getRoom()`、`destroyRoom()`
- [x] 房间码生成：4 位字母数字，确保唯一
- [x] 房间人数限制 2-6 人
- [x] 加入验证：房间不存在、已满、已开始 → 返回对应错误
- [x] 新玩家加入时广播更新后的玩家列表
- [x] Host 断线时自动转移给下一位玩家

> 需求：3.1-3.9 | 属性：3, 5, 20, 23

## 任务 5：回合调度器

- [x] 实现 `TurnScheduler`：`startGame()`、`rollDice()`、`reportJumpResult()`、`endTurn()`、`skipTurn()`
- [x] 游戏开始时：生成 Platform_Seed，确定 Turn_Order（按加入顺序），所有玩家 Platform_Index = 1
- [x] 掷骰子：生成 1-6 随机整数，应用 Dice_Modifier（加值上限 12，最大值截断）
- [x] 跳跃结果处理：成功 → Platform_Index + 1，失败 → 不变，Jump_Quota - 1
- [x] Jump_Quota = 0 时结束回合，轮转到下一位非 Skip_Turn 玩家
- [x] Skip_Turn 处理：自动跳过，skipTurns - 1
- [x] 胜利判定：Platform_Index >= 20 → 设为 20，结束对局

> 需求：2.4, 2.5, 3.8, 6.1-6.14, 10.1-10.4 | 属性：6, 7, 8, 9, 10, 11, 12

## 任务 6：事件仲裁器

- [x] 实现 `EventArbiter` 类：`triggerMysteryPlatform()`、`useItem()`、`applyEvent()`
- [x] Mystery_Platform 触发：70% 概率事件，30% 概率道具
- [x] 10 种竞技事件实现（天灾 2 + 惩罚 3 + 幸运 3 + 对抗 2）
- [x] 防连续相同事件：记录每个玩家上次触发的事件
- [x] 道具使用验证：持有检查、回合检查、蓄力状态检查
- [x] 5 种道具效果实现（必中卡、退后卡、暂停卡、护盾卡、加速卡）
- [x] 道具持有上限 2 个，超出丢弃
- [x] Shield 抵消负面事件后消失
- [x] Platform_Index 变更后 clamp 到 [1, 20]

> 需求：7.2-7.12, 8.1-8.9 | 属性：13, 14, 15, 16, 17

## 任务 7：网络异常处理

- [x] 服务端：玩家断线 5 秒后标记离线，回合自动跳过
- [x] 服务端：所有玩家断线 30 秒后销毁房间
- [x] 服务端：断线玩家重连时恢复状态，发送 `room_snapshot`
- [x] 服务端：Host 断线时自动转移

> 需求：14.4-14.6 | 属性：19, 20

## 任务 8：客户端 WebSocket 网络层 (`NetworkClient`)

- [x] 在 `index.html` 中实现 `NetworkClient` 类
- [x] `connect(url)`：建立 WebSocket 连接
- [x] `send(message)`：发送 JSON 消息
- [x] `onMessage(handler)`：注册消息处理回调
- [x] `startHeartbeat()`：每 2 秒发送心跳
- [x] `reconnect()`：断线后每 2 秒重试，最多 3 次
- [x] 断线提示 UI："连接中断，正在重连..."
- [x] 3 次失败后显示"连接失败"和"返回首页"按钮

> 需求：9.6, 14.1-14.3

## 任务 9：竞技模式入口与房间大厅 UI

- [x] 首页新增"竞技模式"按钮（紫色 #533483），位于经典模式按钮下方
- [x] 点击后检查角色名称（1-8 字符），未设置则要求输入
- [x] 房间大厅界面：创建房间 / 输入房间码加入
- [x] 房间等待界面：显示房间码（大字体可复制）、玩家列表、"开始游戏"按钮（仅房主可见）
- [x] 玩家列表实时更新（响应 `player_list_update` 消息）

> 需求：1.1-1.3, 3.1-3.6

## 任务 10：竞技游戏控制器 (`ArenaGameController`)

- [x] 实现 `ArenaGameController` 类，协调网络消息与游戏引擎
- [x] `initRacetrack(seed)`：调用 `generateRacetrack()` 生成赛道
- [x] `onTurnStart(playerId)`：设置当前回合玩家，显示骰子动画
- [x] `onDiceResult(value)`：设置 Jump_Quota，更新 HUD
- [x] 跳跃流程：复用经典模式蓄力-跳跃-落地判定，目标为 Platform_Index + 1 的平台
- [x] `onJumpBroadcast()`：Ghost 渲染其他玩家跳跃动画
- [x] `onLandResult()`：更新玩家位置，处理 Mystery_Platform 触发
- [x] `onEventTriggered()`：显示事件效果动画
- [x] `onGameEnd()`：显示排名结算界面
- [x] 观战视角：非当前回合玩家观看当前玩家操作

> 需求：5.1-5.8, 6.1-6.12, 9.1-9.7

## 任务 11：竞技 HUD 界面

- [x] 赛道进度条：顶部显示 20 格，各玩家位置用颜色标记
- [x] 当前回合信息：高亮当前玩家名称和颜色
- [x] 骰子与跳跃次数：右上角 "🎲 X | 剩余跳跃：Y"
- [x] 活跃效果列表：骰子信息下方显示效果图标和剩余回合数
- [x] 道具栏：右下角 2 格，显示道具图标和名称，空格虚线边框
- [x] 道具使用交互：点击道具激活，需要目标的弹出选择面板
- [x] 非当前回合玩家道具栏不可点击
- [x] 回合计数：左上角 "第 N 回合"
- [x] 玩家状态卡片：顶部显示所有玩家信息（颜色、名称、位置、暂停状态）

> 需求：11.1-11.9

## 任务 12：Ghost 渲染与多人同步显示

- [x] Ghost 渲染：半透明（0.5）、对应玩家颜色、头顶显示名称（10px 白色）
- [x] 赛道上同时显示所有玩家位置标记
- [x] 回合切换时镜头平滑移动到当前玩家所在平台
- [x] 跳跃广播：当前玩家跳跃时发送消息，其他客户端渲染 Ghost 抛物线动画

> 需求：9.1-9.5, 9.7

## 任务 13：问号平台视觉与事件 UI

- [x] Mystery_Platform 顶面绘制白色 "?" 标记（18px，脉动动画 0.5-1.0 透明度，1.5 秒周期）
- [x] 事件触发时：触发者头顶显示事件名称和图标（1.5 秒淡出）
- [x] 负面事件影响对手时：画面中央红色警告文字（1 秒淡出）
- [x] 监狱效果：角色周围铁栏杆
- [x] 必中卡效果：角色脚下金色光环
- [x] 冻结效果：角色周围冻结视觉
- [x] 退回动画：角色向后滑回

> 需求：7.2, 7.9, 7.10, 8.6-8.9

## 任务 14：竞技模式音效

- [x] 骰子滚动音效：200Hz→600Hz，0.5 秒，正弦波
- [x] 神秘平台音效：300Hz→900Hz，0.4 秒，正弦波
- [x] 警告音效：200Hz 方波，0.3 秒
- [x] 正面音效：523Hz+784Hz，各 0.1 秒
- [x] 胜利旋律：C5-E5-G5，各 0.15 秒
- [x] 道具使用音效：660Hz，0.2 秒
- [x] 回合开始提示音：880Hz，0.15 秒

> 需求：13.1-13.7

## 任务 15：排名结算界面

- [x] 对局结束显示 Rank_Screen，按排名列出所有玩家
- [x] 每位玩家显示：排名、颜色、名称、最终位置、跳跃次数、成功率、事件次数
- [x] 第一名显示金色皇冠和"胜利！"标签
- [x] "再来一局"按钮：返回同房间等待界面
- [x] "返回首页"按钮：回到主菜单
- [x] 排名规则：胜者第一，其余按 Platform_Index 降序，相同则按成功跳跃次数降序

> 需求：12.1-12.6 | 属性：18
